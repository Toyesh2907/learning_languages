//The following file touches upon the basics of c++


/* #include preprocesser directive indicates that we would like to use the contents of the iostream library, which is the part of C++ standard
 library that allows us to read and write text from/to the console.
 without the include std::cout throws a compilation error since it belongs to the iostream library.

 int main() defines a function that takes no function arguments and returns an integer
 std::cout prints stuff on terminal
*/

#include <iostream>
int main(){
  std::cout << "Hello World";
  return 0;
}

